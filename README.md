# Organic Mind — biologically-plausible, online-learning, multimodal AI

Чистый перезапуск исследования (2026-09-01). Предыдущие версии архивированы, не удалены:
`/root/archive/nca_research_v1/`, `/root/archive/biomind_core_v1/`.

## Текущее состояние (конец сессии 2026-09-01) — читать в первую очередь

Полный, детальный лог со всеми числами, найденными багами и их исправлениями —
`docs/VERIFICATION_LOG.md`. Здесь — сжатая сводка, чтобы ничего не потерять
между сессиями.

**8 механизмов реализованы с нуля и проверены** (каждый на игрушечном
масштабе, с явным baseline, никаких "решено" без числа):

| # | Механизм | Файл | Статус |
|---|---|---|---|
| — | PC-релаксация (замена backprop) | `core/predictive_coding.py` | ✅ 91.1% vs 98.9% backprop, работает, ~15x медленнее |
| 1 | Растущий структурный субстрат | `core/growing_tissue.py` | ✅ +33.3 п.п. регенерация после абляции |
| 2 | SDR-гиппокамп | `core/sdr_hippocampus.py` | ✅ с поправкой к v1 (падает с масштабом: 100%→18% при N=50→4000) |
| — | VSA-binding | `core/vsa_binding.py` | ✅ ёмкостная кривая до K=512, не "решено" |
| 3 | Сон/консолидация | `experiments/sleep_generalization_sanity.py` | ✅ пересобран вокруг схема-vs-исключения (CLS-теория) |
| 4 | Гомеостаз (BCM) | `core/growing_tissue.py`, `core/unified_organism.py` | ✅ апоптоз: скромный эффект; порог роста: percentile-самонастройка, подтверждена A/B на Colab (константа не работала на масштабе, percentile — работает) |
| 5 | Колонки+голосование | `core/columnar_voting.py` | ✅ самый чистый результат (+27.7 п.п. на K=4) |
| 6 | JEPA-понимание | `experiments/jepa_understanding_sanity.py` | ✅ сильный результат (+33.3 п.п.), но словарь ~30 слов |
| 7 | Эволюция ABCD-правила | `core/evolved_hebbian.py` | 🟡 приостановлено, не закрыто — попробованы ОБА протокола (single-task и multi-task эволюция, литературный подход): ни один не даёт устойчивого превосходства над ручной beta при переносе (5 честных trial'ов, среднее±std multi=74.7±6.7% vs hand=72.3±1.8% — разница тонет в разбросе) |
| 8 | Дендритная компартментализация генома | `core/predictive_coding.py` (`w0_mask`) | ✅ честный компромисс на реальном MNIST — 88.4%→80.5%/75.0%/75.4% при 25%/6.2%/1.6% параметров первого слоя, точность ПЛАТО между 4x4 и 7x7 патчами (основная потеря на грубой компартментализации, не на мелкой) |
| 9 | Wiring cost (мягкий коннектомика-штраф) | `core/predictive_coding.py` (`wiring_cost`) | ✅ перекрёст с дендритной маской — заметно эффективнее hard-маски в умеренном диапазоне сжатия (86% разреж./85.1% против hard 4x4 93.8%/75.0%), но деградирует БЫСТРЕЕ на экстремальной разреженности (>96%) — вероятная причина: случайные позиции могут оставить отдельных нейронов почти без сильных связей, hard-маска гарантирует непустое рецептивное поле каждому |
| 10 | Synaptic Intelligence (защита от забывания) | `core/predictive_coding.py` (`si_enabled`) | ✅ первый прямой тест "запоминания" из центрального обещания проекта — baseline даёт драматичное забывание (94.5%→13.5% после второй задачи), SI даёт ЧИСТУЮ монотонную кривую компромисса стабильность-пластичность по 5 значениям si_lambda (забывание 81.0→39.0 п.п., точность новой задачи 89.0%→44.0%) — реально снижает забывание, не устраняет полностью, ожидаемый trade-off, не "решено" |

**Первое слияние в единый организм** — `core/unified_organism.py`
(рост + обучение генома + быстрая память вместе, впервые не в разных файлах):
- ✅ Рост стабилен и **самонастраивается под масштаб** (percentile-порог вместо константы — доказано A/B на Colab: константа не работала на холсте ×28, percentile работает без изменений)
- ✅ Геном учится (ошибка предсказания падает; ранний диагноз "не сходится" оказался ошибкой измерения)
- ✅ Память с приоритетом по силе метки (synaptic tagging) — сильный эффект, направление держится и на N=400 (26.5% vs 0.5%)
- ✅ Регенерация после повреждения — через локальное "воспаление" (временная сенсибилизация раны, затухает, не портит стабильность роста), усиливается на масштабе (18→39 на Colab)
- ✅ **Проверено на Colab T4 GPU** — 128×128 холст (×28 к локальному), работает без перекалибровки
- ✅ **Колонки/голосование слиты** — `add_modality`/`write_fact_modal`/`read_fact_voted` (см. `docs/VERIFICATION_LOG.md`): организм умеет писать один факт под несколькими независимыми модальностями и читать confidence-weighted голосованием (без ручного веса на модальность), проверено на N=40 фактов, включая тест устойчивости к зашумлённому каналу (sigma 0.3-1.5) — voted стабильно ≥ naive-average ≥ деградировавшей одиночной модальности
- 🟡 JEPA-понимание — инфраструктура смерджена (`compute_context`/`hidden_representation`), но downstream-тест ("граница vs внутри клетка" на held-out после абляции) дал СМЕШАННЫЙ результат при переборе grow_steps 300/800/1500/3000 (пробник выигрывает не всегда, при самой большой/сбалансированной популяции проигрывает hardcoded-правилу) — не подтверждено консистентно, см. VERIFICATION_LOG
- ✅ **VSA-binding слит** — `init_vsa`/`write_compositional`/`read_compositional`: одна SDR-запись на объект (роли связаны через circular convolution) стабильно и заметно (14-56 п.п.) обгоняет K отдельных flat-записей по всему диапазону K=4..32, самый чистый и устойчивый мердж-результат сессии — см. VERIFICATION_LOG
- 🟡 phase-binding (binding-by-synchrony, Kuramoto) — реализован с нуля `core/phase_binding.py`, механизм математически подтверждён (идеальный случай: точная сегментация r=0.000). Мердж в организм (`init_phase`/`phase_sync_step`) сделан, но найдено АРХИТЕКТУРНОЕ противоречие: релаксация химии организма (через sobel/laplacian-контекст) диффузионна по конструкции и АКТИВНО сглаживает межобластные различия (обучение под разными сигналами дало cos_sim=1.000, ХУЖЕ случайного шума cos_sim=-0.308 для сегментации) — открытый вопрос, не решено

**Честно не сделано, зафиксировано как открытое:**
1. ✅ M(-1) (scaling study) — ЗАВЕРШЁН симметричной проверкой: PC и BP оба честно настроены (перебор гиперпараметров с обеих сторон, включая lr у BP на hidden=512 и hidden=4096). Финальный вывод: разрыв держится 6-10 п.п. без систематического сужения, слегка растёт на больших бюджетах (PC плато ~36%, BP растёт до 46.2%). Прошлый вывод "разрыв сужается до 2.6 п.п." был артефактом ненастроенного BP baseline (lr=0.003 вместо оптимального 0.0003) — отозван. Позиция "не соревнуемся по качеству" подтверждена с максимальной доступной нам строгостью. **Третье независимое подтверждение** (CPU follow-up, Colab был недоступен — см. п.5b ниже): разрыв держится 5.9-7.1 п.п. на hidden 512/768/1024 даже при сокращённом бюджете, где абсолютная точность обеих сторон ПАДАЕТ с ростом hidden (не растёт, как на Colab) — постоянство разрыва не зависит от режима/бюджета
2. Масштаб всё ещё маленький в абсолютных числах (десятки клеток, сотни фактов) — Colab доказал, что *механизмы* переносятся на больший холст, но не то, что вся система готова к реальному масштабу
3. ✅ частично — первое реальное сенсорное подключение сделано (настоящий MNIST, `core/mnist_loader.py`). PC vs BP: разрыв устойчиво мал (+0.2..+2.6 п.п. в пользу BP, среднее +1.79±0.75 п.п. по 9 прогонам, N_train∈{1000,2000,4000}×3 seed, все 9 положительны) — заметно и стабильно меньше 6-10 п.п. на синтетике M(-1). Архитектура/датасет пока фиксированы (784->128->10, только MNIST) — не проверено на других (см. VERIFICATION_LOG)
4. ~~Colab/GPU ни разу не использован~~ — ✅ сделано, см. ниже
5. 🟡 частично — устойчивость колонок к шуму подтверждена (N=40, sigma 0.3-1.5); устойчивость генома/роста к шуму vs состязательному потоку протестирована (4 seed), результат НЕ подтверждён и НЕ опровергнут — делится ровно по seed, нужна более сильная атака/больший масштаб (см. VERIFICATION_LOG)
6. 🟡 первый тест сделан, продлён до 15000 шагов (rule 1 — не выводить из одного горизонта): нет коллапса/взрыва/истинного расхождения ошибки генома, НО то, что на 6000 шагах выглядело как плато населения (183, стабильно) оказалось ВРЕМЕННЫМ — рост возобновился позже (189→290 за последние 3000 шагов), ошибка генома дала заметный (2.4x) но самовосстановившийся всплеск в середине горизонта. Recall памяти при N=119 (53.8%) неотличим от естественного разброса SDR-ёмкости (проверено на 4 seed: 48.7-65.5%). Один прогон, недели реальной работы (M6 в полном смысле) всё ещё не тестировались
7. Ни одного прямого сравнения с реальным LLM по осям из ROADMAP M5

**Приоритет следующих шагов** (см. также `docs/ROADMAP.md`):
1. ~~Масштабировать `unified_organism.py` на Colab GPU~~ — ✅ сделано: 128×128 холст (×28), percentile-порог роста самонастраивается под масштаб без перекалибровки (константа — нет, доказано A/B)
2. Слить оставшиеся компоненты в организм — колонки ✅, VSA-binding ✅ (оба - см. выше); JEPA-понимание 🟡 инфраструктура готова, downstream-тест не подтверждён консистентно; phase-binding 🟡 смерджен, но найдено архитектурное противоречие с диффузионной релаксацией химии (см. выше) — весь список из README теперь пройден, с разной степенью успеха
3. ~~Запустить M(-1)~~ — ✅ сделано
4. ✅ начато — реальный MNIST подключён и к изолированному PC-net сравнению, и к самому unified_organism.py (цифра как персистентный сенсорный стимул). Неожиданная, устойчивая (8/8 цифр) находка: геном организма показывает ВЫШЕ ошибку предсказания на реальной цифре, чем на shuffle-шуме с идентичной статистикой — первая гипотеза (локальная дисперсия) проверена и опровергнута собственными данными, причина пока НЕ объяснена, зафиксирована честно как открытый вопрос (см. VERIFICATION_LOG)
5. Устойчивость к шуму — 🟡 частично протестировано: голосование колонок устойчиво к шуму в ОДНОЙ модальности (N=40, sigma 0.3-1.5, консистентно). Устойчивость генома/роста к шуму vs состязательному потоку — протестирована ДВУМЯ независимыми методологиями атаки (single-step gradient и momentum-накопленная, стандартная в adversarial ML), обе на 4 seed: ОБЕ дают один и тот же результат — adversarial НЕ хуже (даже чуть мягче) случайного шума той же нормы, momentum не усиливает атаку. Дважды подтверждённый негативный результат на цели "ошибка генома". Атака НЕПОСРЕДСТВЕННО на решение о росте (percentile-порог) — подтверждена на N=12 seed (после N=4): равномерная атака НЕ увеличивает население (15.9 vs noise 15.6 — согласуется с относительной, самонормализующейся природой порога), целевая (концентрированная) атака ДАЁТ реальный, воспроизводимый эффект (население 17.6 vs 15.6, в целевой зоне 15.4 vs 12.5 — величина эффекта, +2.0, почти точно совпала между N=4 и N=12, в отличие от нескольких других находок сессии, растворившихся в шуме). Организм устойчив к равномерным манипуляциям ростом (как задумано конструкцией), но НЕ полностью устойчив к пространственно-целевым атакам. Первый прогон поймал методологический баг (недостижимая целевая зона — рост требует смежности с уже живыми клетками, атака не может "телепортировать" рост) — см. VERIFICATION_LOG. Долгая непрерывная работа (M6) — 🟡 первый тест сделан, продлён до 15000 шагов, структурно стабильно (но не в истинном равновесии, см. п.6 выше)

Честная оценка масштаба: это проверенный фундамент, не работающая система.
Аналогия с Hopfield (40 лет от публикации до признания) — реалистичный
порядок величины для такой работы, не то, что делается за недели соло.

## Методология

**Ничего готового.** Библиотеки-инструменты (PyTorch как язык тензорных
операций) — можно. Готовые реализации самой архитектуры мышления (чужие
NCA-фреймворки, готовые HTM/equilibrium-propagation библиотеки) — нельзя.
Подглядывать в статьи можно и нужно, но каждый механизм — своя реализация
с нуля, со своим тестом, прежде чем переходить к следующему. Это касается
и внешних статей из roadmap (не только кода v1/Hermes): claim из статьи
(Whittington & Bogacz, BCM, genomic bottleneck и т.д.) переходит в статус
"подтверждён" только после того, как мы сами реализовали и перепроверили
его — и, где возможно, попытались улучшить, а не просто процитировать.
Пример: `core/predictive_coding.py` — не импорт готовой PC-библиотеки,
а собственная реализация с честным сравнением против backprop. Проверяем
каждую подсистему отдельно, шаг за шагом — не собираем систему из частей,
которые никто не проверил по отдельности.

## Что было бы настоящим научным вкладом (не просто рабочим прототипом)

Прецедент: Нобелевка по физике 2024 (Hopfield/Hinton — фундаментальные
принципы нейросетей), по химии 2024 (Hassabis/Jumper/Baker — AlphaFold),
по медицине 2014 (O'Keefe, Moser & Moser — place/grid cells, механизм
пространственной памяти). Общий паттерн: (1) общий механизм, решающий
ранее открытую проблему, (2) математически охарактеризован, не просто
эмпирически подкручен, (3) валидирован дважды — как инженерное решение
И против реальных данных природы (AlphaFold проверяли на реальных
кристаллографических структурах, не только на бенчмарках).

Наша версия этого стандарта — три критерия для каждого крупного
результата, не только "работает на N примерах":
1. **Решает credit assignment без глобального градиента на реальном
   масштабе**, не toy-задаче. Whittington & Bogacz доказали математическую
   эквивалентность backprop локально — никто не показал, что это
   масштабируется до реальной компетенции. Это сейчас открытая проблема.
2. **Предсказывает реальные данные нейронауки количественно**, не просто
   "выглядит похоже". Пример: кривые забывания/консолидации системы
   должны совпадать с реальными экспериментальными кривыми (начиная
   с классических кривых забывания), не только между собой.
3. **Достаточно общий механизм, чтобы был переиспользуем за пределами
   этого проекта** — как backprop, attention, Hopfield-сети.

Честная оговорка: это горизонт на годы с рецензированием и независимым
воспроизведением, не соло-результат за недели. Не путать "работающий
прототип" (наш текущий уровень строгости) с "доказанным вкладом"
(следующий уровень, к которому стремимся).

## Цель проекта

Не конкурировать с LLM по бенчмаркам качества (BPC/perplexity). Цель — настоящий
**органический ИИ**: обучается динамически, на лету, без backprop в момент жизни,
запоминает мультимодально, растёт и чинит себя структурно — как реальный мозг,
а не как склеенные вручную "органоиды" с константами, подобранными разработчиком.

## Что подтвердилось в v1 (сохранить, переиспользовать идею — не код 1:1)

- **Онлайн-обучение без backprop реально работает.** Хеббовское правило
  `W += beta * outer(error, sparse_key)`, применяемое прямо во время
  взаимодействия (не в фазе тренировки) — рабочий, воспроизводимый механизм.
  (было в `active_predictive_cortex.py`, `bicameral_organism.py`)
- **Разреженное кодирование (SDR, kWTA ~5-6% активных)** снимает интерференцию
  Хеббовской памяти при росте числа фактов — устойчиво показано на 80+ фактах.
- **Единый "геном" на все клетки ткани** (одна и та же локальная MLP-функция
  применяется идентично к каждой клетке; рост, апоптоз и память выводятся из
  ОДНОГО и того же сигнала активности) — архитектурно честнее, чем отдельные
  hand-built классы под "гиппокамп"/"кору"/"V1"/"A1". Лучший пример был в
  `spatial_embodied_tissue.py`.
- **Латеральная 3D/2D связность** даёт устойчивость к структурным повреждениям
  (>75% функции сохраняется после 30-50% абляции) — то, чего не бывает у
  плоских MLP/Transformer.
- **Сон/кристаллизация реально работают, не только в отчётах.** День:
  Хеббовская запись в `W_fast` (без backprop). Ночь: generative replay
  (включая сэмплированные старые факты — настоящая защита от забывания),
  затем `W_fast` обнуляется — временный след становится постоянным весом.
  Живой честный A/B тест (`phase3_thalamus_and_sleep.py`) подтверждает
  эффект без сна/со сном в одном прогоне.
  (`phase3_thalamus_and_sleep.py`, `phase3_generative_replay.py`)

### Что нужно решить осознанно при слиянии в M0 (не унаследовать молча)

- **Ночная консолидация сейчас использует обычный backprop+AdamW**, не
  локальное правило — прямое противоречие цели "без backprop". Нужно либо
  заменить на локальное predictive-coding-совместимое правило, либо
  осознанно решить, что backprop-only-во-сне допустим (в реальном мозге
  консолидация тоже пластичность, но локальная, не BPTT).
- **Апоптоз/рост (seed_tissue) и сон/кристаллизация (bicameral) — в разных,
  несовместимых файлах.** Тот же паттерн раздробленности, что и с
  `organic_multimodal_mind.py`/`morphogenetic_3d_cortex.py` — растущая
  ткань и день-ночной цикл памяти никогда не были одним организмом.
- **Не все числа в отчётах v1 пересчитаны в том же прогоне** — в
  `phase3_generative_replay.py` сравнительные цифры ("5.6%", "0.3%")
  захардкожены в print, не вычислены live. Не доверять таким числам без
  переисполнения.

## Что было неправильно в v1 — не повторять

1. **Хардкод-константы вместо саморегуляции.** Пороги нейрогенеза/апоптоза/
   пластичности были магическими числами, разными и рассинхронизированными
   в разных файлах (0.6 / 0.25 / 0.3 для одного и того же по смыслу порога).
   Решение — гомеостатическая метапластичность (см. роадмап п.3).
2. **"Понимание" = python if/elif, а не выученное поведение.** В диалоговом
   движке intent-парсинг был захардкожен по позиции токена; сеть-"кора"
   существовала, но не вызывалась в этой логике. Решение — предиктивная
   самонадзорная цель вместо скриптового парсера (роадмап п.5).
3. **Ручная склейка модальностей.** V1/A1/text — отдельные заранее
   спроектированные классы, объединённые константами (`x + y*0.3`).
   Решение — множество идентичных колонок + голосование (роадмап п.4)
   вместо bespoke энкодера под каждую модальность.
4. **Отсутствие git-дисциплины.** Весь v1 — 3 коммита пост-фактум на
   сотни экспериментов; реальная история проб/ошибок нигде не сохранена.
   → В этом проекте: коммит после каждого эксперимента, с самого начала.
5. **Дублирование архитектур без слияния.** nca_research и biomind_core
   параллельно переизобретали одну и ту же идею (`columnar_3d_cortex.py`
   существовал в двух несовместимых версиях). → Один core/, без форков.
6. **Отчёты завышали значимость toy-масштаба.** "Решена the binding problem",
   "решена catastrophic forgetting" — на 10-20 сущностях/80-150 фактах.
   → В этом проекте формулировка результатов: "продемонстрирован эффект
   на N сущностей/фактов", масштаб указывается всегда, никаких "решено".

## Роадмап: 6 конкретных направлений (со ссылками)

1. **Эволюционируемые/метаобучаемые правила пластичности** — коэффициенты
   ABCD-правила Хебба (`Δw = A·pre·post + B·pre + C·post + D`) находятся
   внешним циклом (эволюция/метаградиент), а не подбираются руками.
   - Najarro & Risi, *Meta-Learning through Hebbian Plasticity in Random
     Networks* — https://arxiv.org/pdf/2007.02686
   - *Evolving Decomposed Plasticity Rules for Information-Bottlenecked
     Meta-Learning* — https://arxiv.org/abs/2109.03554
   - *Hebbian and Gradient-based Plasticity Enables Robust Memory and
     Rapid Learning in RNNs* — https://arxiv.org/pdf/2302.03235

2. **Genomic Bottleneck** — маленькая "геномная сеть" генерирует веса
   (и пороги/гиперпараметры) большей сети-организма; сжатие само по себе
   регуляризатор, улучшает transfer.
   - Pedersen & Risi, *Encoding innate ability through a genomic
     bottleneck*, PNAS 2024 — https://www.pnas.org/doi/10.1073/pnas.2409160121

3. **Гомеостатическая метапластичность (BCM-правило)** — порог LTP/LTD
   скользит по собственной истории активности клетки (EMA), а не
   фиксированная константа. Самое дешёвое и приоритетное направление.
   - *Metaplasticity: Unifying Learning and Homeostatic Plasticity in
     Spiking Neural Networks* — https://www.researchgate.net/publication/373332869
   - Turrigiano, *The Self-Tuning Neuron: Synaptic Scaling of Excitatory
     Synapses*, Cell — https://www.cell.com/fulltext/S0092-8674(08)01298-1

4. **Thousand Brains Theory / Monty (Numenta, 2025)** — много идентичных
   полунезависимых колонок (по одной на модальность + внутренние), каждая
   учится через движение/референс-фреймы; результат — голосование/консенсус,
   а не жёсткая формула слияния. Замена ручной склейке V1+A1+text.
   - https://www.emergentmind.com/topics/thousand-brains-systems
   - *The Thousand Brains Theory 2.0* — https://arxiv.org/html/2507.05888

5. **JEPA / V-JEPA2 (LeCun/Meta, 2025)** — самонадзорное предсказание
   в пространстве представлений (без реконструкции пикселей, без
   размеченных интентов). Замена скриптовому if/elif "пониманию":
   понимание — побочный продукт того, что помогает предсказывать
   собственное будущее/скрытую модальность.
   - https://kodu.ut.ee/~hadachi/Lecture_Notes/JEPA.html
   - Обзор проектов — https://github.com/AbdelStark/awesome-jepa

6. **Связывание через фазовую синхронизацию (гамма-осцилляции)** —
   биологический механизм binding problem: признаки связываются через
   синфазность колебаний между зонами коры, а не через VSA/circular
   convolution (математический трюк, которого нет в мозге).
   - *Synchronization of Sensory Gamma Oscillations Promotes Multisensory
     Communication* — https://www.ncbi.nlm.nih.gov/pmc/articles/PMC6873160/

### Приоритет запуска
Начинать с **п.3 (BCM-пороги)** — дёшево, быстро проверяется, напрямую
убирает хардкод. Затем **п.1 (эволюция ABCD-правила)** — самое содержательное
для цели "учится сама, а не по script". Остальное — по мере готовности
базового субстрата.

## Инфраструктура

- **Локально GPU нет** (`nvidia-smi` отсутствует в этой машине).
- **Google Colab доступен через CLI** — `colab` (google-colab-cli, авторизован).
  Полезные команды: `colab new` (поднять GPU-сессию, был использован T4),
  `colab exec` / `colab ssh` / `colab run`, `colab stop` (обязательно
  останавливать после работы, чтобы не жечь compute units зря).
  Сессии по умолчанию не персистентны — если сессия остановлена, состояние
  на VM теряется, если явно не сохранено (`colab download`) или не примонтирован
  Google Drive (`colab drivemount`).

## Структура репозитория

```
organic_mind/
├── core/         # Единый субстрат ткани — без параллельных форков
├── experiments/  # Каждый эксперимент — отдельный коммит
├── docs/         # Живая документация решений и результатов (с указанием масштаба N)
└── README.md     # Этот файл
```

## Правило коммитов
Коммит после каждого законченного эксперимента, сообщение — что проверялось
и результат с масштабом (например: "BCM-порог нейрогенеза: N=200 клеток,
устойчиво в диапазоне stress 0.1-0.9"). Не "решено", а "проверено на N".
